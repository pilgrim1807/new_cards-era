{
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Debug portfile(s)",
            "preLaunchTask": "Debug vcpkg commands"
        }
    ]
}

